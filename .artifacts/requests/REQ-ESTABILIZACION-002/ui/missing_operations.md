# Missing Operations

## Tesoreria
- **Recibos List**: No dedicated list view found in router for `/recibos` (only `/recibos/nuevo`). Recibos might only be visible inside Client Current Account.

## Facturacion
- **Invoice Detail**: No dedicated view `/comprobantes/:id` detected in router, uses inline expand or modal? `InvoiceListView` has a "Ver" link but it just implies navigation or modal (Need to verify).

## General
- `404` View not explicitly found in router (redirects to home?).
