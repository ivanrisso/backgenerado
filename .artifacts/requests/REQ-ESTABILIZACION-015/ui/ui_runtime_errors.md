# UI Runtime Errors

## Summary
Scan executed with role **ADMIN** using strict **Anti-Stale** `ui-runtime-menu-scan` skill.
Protocol required returning to Dashboard before every navigation to prevent stale DOM references.

## Observations
- **Session Timeout:** Occurred during `Tesorería > Nuevo Recibo`. Recovered via re-login. Handled correctly.

## Critical
- None.
