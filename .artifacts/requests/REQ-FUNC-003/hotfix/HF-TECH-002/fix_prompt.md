# Fix Prompt — HF-TECH-002

## Contexto
Broken Menu Links (Dashboard, Menus).

## Instrucción
1. Fix `/dashboard` link: Ensure it points to `/` or alias `/` to `/dashboard`.
2. Fix `/menu-items` link: Change to `/menus` (which exists in router).
3. Verify navigation works.
